############################
# POOLMAN GLOBAL PROPERTIES
############################
#
# Support for multiple pool types is accomplished through
# the [n] arguments in the parameter names
#
#
############################
# OPTIONAL PARAMETERS:
############################
#
# maximumsize.n=[put an integer denoting the maximum pool size here]
# connection_timeout.n=[number of milliseconds each connection should stay alive]
# checkfrequency.n=[put number of milliseconds here]
#
#
############################
# CACHE PARAMETERS:
############################

# enableCache.n=[set to "true" if using a cache]
# cacheSize.n=[indicate the maximum size of the SQL cache (default is 100)]
# cacheRefresh.n=[indicate the interval, in milliseconds, when the cache should refresh (default is 300000, which is 5 minutes)]
############################

db_name.1=PlannerDb2
db_driver.1=sun.jdbc.odbc.JdbcOdbcDriver
db_url.1=jdbc:odbc:PlannerDb2
db_username.1=
db_password.1=
enableCache.1=true
cacheSize.1=5
cacheRefresh.1=100000
