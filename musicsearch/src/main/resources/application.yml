server:
  port: 80
#  compression:
#     enabled: true
#     mime-types: text/html,text/xml,text/plain,text/css,application/json,application/javascript,image/svg+xml,application/x-font-ttf,application/x-font-truetype,application/x-font-opentype,application/vnd.ms-fontobject,application/font-sfnt  

spring:
  resources:
    cache:
      period: 31556926
  mvc:
    favicon:
      enabled: false

app:
  music-dir: /opt/music_library
  index-dir: /opt/music_ix
  nginx-send-file-context: /musiclib

logging:
  level:
    org.springframework: WARN
    org.apache: WARN  
    org.jaudiotagger: ERROR
    ch: ERROR
